import React from "react";
import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <div>
      {/* <nav className="nav">
        <Link to="/" className="site-title">
          Site TodoList
        </Link>
        <ul>
          <Link to="/Filter">Filter</Link>|
          <Link to="/ContactUs">Contact Us</Link>|<Link to="/login">Login</Link>
        </ul>
      </nav> */}
    </div>
  );
};
export default Navbar;
